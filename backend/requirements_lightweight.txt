# Lightweight Backend Requirements for 8GB RAM
# Optimized for Chatterbox TTS + Voice Cloning

# Core FastAPI and server dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
aiofiles==23.2.1
python-dotenv==1.0.0

# Chatterbox TTS - Production-grade open source TTS with voice cloning
chatterbox-tts==0.1.4

# Core ML libraries (compatible with Chatterbox)
torch>=2.1.0,<=2.6.0
torchaudio>=2.1.0,<=2.6.0
transformers==4.46.3
numpy>=1.24.0,<1.26.0
librosa==0.11.0
soundfile==0.12.1

# Audio processing utilities
pydub==0.25.1
scipy>=1.11.0

# Optional: Basic offline TTS fallback (no ML required)
pyttsx3==2.90

# System utilities
requests==2.31.0
httpx==0.25.2
pillow==10.1.0

# Development (optional)
pytest==7.4.3
pytest-asyncio==0.21.1
